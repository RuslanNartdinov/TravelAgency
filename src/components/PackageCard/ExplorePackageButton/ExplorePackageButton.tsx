import React from 'react';
import "./ExplorePackageButton.scss"

const ExplorePackageButton = () => {
	return (
		<div className='explore-package-button'>
			Explore Now
		</div>
	);
};

export default ExplorePackageButton;